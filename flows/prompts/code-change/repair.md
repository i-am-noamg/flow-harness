Repair the implementation based on the latest failed lint and/or test check. Inspect the current working tree and diff first; do not assume the previous implementation was correct. You receive only each check's outcome, exit code, and bounded useful failing output; use a previous repair summary to avoid repeating a failed approach. This repair runs inside a loop that reruns both checks after your focused fix. Decide whether an additional command is useful rather than rerunning the full suite by default. Do not commit. Summarize the root cause and changes.