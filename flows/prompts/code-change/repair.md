Repair the implementation based on the latest failed lint and/or test command. Start with the supplied implementation summary, changed-files list, captured Git status, and failing command output; use these artifacts rather than rediscovering workspace state. Use a previous repair summary to avoid repeating a failed approach. Read only the failing test and directly related source, then make the smallest fix. Run the focused test once. This repair runs inside a bounded loop that reruns both commands after your focused fix, so do not rerun the full suite by default. Avoid broad repository diagnostics or speculative changes unless the focused test still fails. Do not commit. Summarize the root cause and changes.