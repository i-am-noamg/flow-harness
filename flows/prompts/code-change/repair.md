Repair the implementation based on the latest failing test output. Inspect the current working tree and diff first; do not assume the previous implementation was correct. If a previous repair summary is provided, use it to avoid repeating the same failed approach. This repair runs inside a loop: after you finish, the workflow will run `npm test` at the start of the next iteration. Decide whether any additional command or test is useful before making the focused fix, rather than rerunning the full suite by default. Do not commit. Summarize the root cause and changes.