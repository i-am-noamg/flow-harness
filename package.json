{
  "name": "flow-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "flow": "dist/cli.js" },
  "scripts": { "build": "tsup", "dev": "tsx src/cli.ts", "typecheck": "tsc --noEmit", "test": "npm run typecheck" },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
